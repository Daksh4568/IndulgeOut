# SPA Routing Support - Redirects all routes to index.html
# This ensures React Router works correctly on AWS Amplify and prevents 404 errors
# on direct URL access (like payment-callback)
# 404 status means: only redirect when file doesn't exist (won't redirect .js, .css, images)

/*    /index.html   404